input:
  generate:
    interval: 1s
    count: 5
    mapping: |
      root = "hello from redpanda connect"
pipeline:
  processors:
    - yell:
        prefix: ">> "
        repeat_count: 2
output:
  stdout:
    codec: lines
